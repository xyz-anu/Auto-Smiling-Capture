# Auto-Smiling-Capture
A learning focused computer vision miniproject that uses OpenCV and MediaPipe FaceMesh to detect smiles in real time and automatically capture images, with emphasis on macOS-based Python environment setup and debugging.
